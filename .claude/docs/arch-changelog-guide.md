# Architectural Changes Guide

Create an entry when you change architecture (data flow, patterns, schema strategy, global state, major subsystem refactors).

Use the directory referenced by `CLAUDE.md` and follow the project’s naming convention.

